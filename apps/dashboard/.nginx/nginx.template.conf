server {
  listen 80;
  server_name _;

  root ${NGINX_ROOT_DOCUMENT};
  index index.html;

  location = / {
    default_type text/plain;
    return 200 'ok';
  }

  location / {
    try_files $uri $uri/ /index.html;
  }

  location ~ ^/[^/]+(/.*)$ {
    try_files $1 $1/ /index.html;
  }
}
